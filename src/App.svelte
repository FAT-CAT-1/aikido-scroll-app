<main class="cover">
  <h1 class="title">合氣道</h1>
</main>

<style>
  .cover {
    min-height: 100dvh;
    display: grid;
    place-items: center;
    padding: var(--space-5);
  }

  .title {
    writing-mode: vertical-rl;
    font-family: var(--font-heading);
    font-size: clamp(3rem, 18vw, 7rem);
    letter-spacing: 0.2em;
    color: var(--sumi-shou);
  }
</style>
