# 実装判断ログ（docs/decisions.md）

> ディレクター未回答の質問（初回報告 C-Q1〜Q5）と、文書間の矛盾・曖昧さ（初回報告 B-1〜28）に対して
> 実装担当（Opus）が置いた**仮置き**の記録。変更したい場合は ID を指定して指示してください。
> 上位文書（content-spec / animation-spec / backlog / design-complete）を書き換えるものではありません。

| ID | 論点 | 決定（仮置き） | 根拠 |
|---|---|---|---|
| D-01 | 公開範囲（Q1） | GitHub の public リポジトリで公開。`docs/references/*.txt`（第三者動画の文字起こし全文）は `.gitignore` で除外しローカル保持 | Pages 無料枠は public 必須。全文の再配布を避ける |
| D-02 | コミット作成者（Q2） | リポジトリローカル設定で `FAT-CAT-1` ／ GitHub noreply アドレス | 個人メールを公開履歴に残さない |
| D-03 | フォント（Q3） | google/fonts（OFL・RFN なし）の固定コミットから原本を取得し sha256 検証 → `subset-font` で woff2 化。書体の役割ごとに収録文字を分ける：Yuji Syuku＝見出し・技名・用語名・UI の文字／Shippori Mincho 400＝第1水準＋原稿／Shippori 700・Zen Old Mincho＝原稿の文字。合計約 1.6MB。原稿追加でサブセット外の文字が出たら `npm run fonts` | 全量 4.1MB → 1.6MB。3G での初回表示と precache 容量。Shippori Mincho は B1 ではない方 |
| D-04 | PWA の完了条件（Q4） | Lighthouse v12 で PWA カテゴリが廃止されたため、「DevTools > Application > Manifest の警告 0 ＋ Service Worker activated ＋ 機内モード起動」に置換。Performance / Accessibility 90+ は維持 | 計測不能な基準を残さない |
| D-05 | 説明のみの kf（Q5） | 全 kf で 取り/受け × 6部位 × l1〜l5 を必須（kamae も書く） | content-spec §6 のエラー条件・backlog T06 と一致 |
| D-06 | 無印（一般的な指導）の文字色 | `--trust-general` を `--sumi-tan`(#8a8a8a) ではなく `--sumi-juu`(#555) に。事実＝焦墨、師範差＝朱。色以外の手掛かりとして文末に小さな印（事／師）を付ける | l2〜l5 の大半が無印文で、#8a8a8a は和紙上 2.8:1 と本文 4.5:1・axe 0 を満たせない。B-4「値は上書き可」 |
| D-07 | 依存追加のルール | 「1タスク2個まで・追加理由を1行」。公式テンプレート相当の雛形（T02）と、design-complete A-2 / A-11 が名指しする依存は例外 | SKILL「1PR 2個まで」は main 直 push 運用と合わないため |
| D-08 | CI の `build:content` | `npm run build:content --if-present`（T08 までスキップ） | A-10 の手順を固定したまま T04 を先行できる |
| D-09 | manifest の置き場所 | `vite.config.ts` の VitePWA 設定で定義（`public/manifest.webmanifest` は置かない） | 二重定義を避ける |
| D-10 | PWA アイコン | 自作の仮 SVG（和紙地に墨の円相）から PNG を生成してコミット。本デザインは E10 | 仕様未定義 |
| D-11 | manifest の色 | `background_color` #efe8d8（和紙）／`theme_color` #1a1a1a（焦墨） | A-9「テーマ色 --washi/--sumi-shou」の読み |
| D-12 | 画面の向き | 仕様どおり `orientation: portrait`（WCAG 2.2 SC 1.3.4 に抵触しうる点は要判断として残す） | A-9 |
| D-13 | Node | CI は Node 22、`engines` は `>=22.12.0`（Vite 8 の要件） | backlog §0 |
| D-14 | TypeScript | `~6.0`（svelte-check 4.7 が TS 7 未対応。Vite 公式テンプレートと同じ） | 互換性 |
| D-15 | 一教（表）の kf | `kamae 0.0 / contact 0.15 / kuzushi 0.35 / irimi 0.55 / osae 0.8 / zanshin 1.0`（原稿・pose 共通） | backlog T06 の目安6kf、content-spec §8 の at を踏襲 |
| D-16 | 事実マークの出典 | `{{f:}}` は frontmatter の sources に実在出典を定義して `@src` を付けられる文だけ。出典を示せない事柄は無印か `{{v:}}` | 警告0と正確さの両立。架空出典を作らない |
| D-17 | 動画リンク | 講師・内容を確認できるまで `videos: []` | 未確認 URL を載せない |
| D-18 | `[[用語]]` の解決先 | 単語集（name_ja / aliases）を優先し、無ければ技・基礎（name_ja / aliases）へ `#/techniques/:id` でリンク | content-spec §2-2 の関連欄 `[[一教（裏）]]` が技を指すため（初回報告 B-20） |
| D-19 | wiki-link の実装 | `@portaljs/remark-wiki-link` は使わず、ビルド時の自前トークナイザで処理（remark の mdast 上で変換） | 同プラグインは `[[対象\|表示]]` 順で、content-spec の `[[表示\|正規名]]` と逆。信頼マークと同時に処理するため |
| D-20 | l1・文字数 | l1 は表示文字だけを出力（`[[ ]]` やマークは警告して除去）。文字数は記法と出典表記を除いた表示テキストで数える | 初回報告 B-21, B-22 |
| D-22 | 巻物のスクロール方向 | トップの巻物は左→右（最初の章が左端）。章題は各章の右端に縦書き。`ScrollStage` の並び方向を変えれば右→左（絵巻の本来の向き）にもできる | 初回の横スワイプで迷わせない（部員テスト台本の「説明なしで分かる」）。技の再生位置（時間）と同じ向きにそろえる |
| D-23 | 章扉の文言 | 章題（UI）以外は、原稿 `content/pages/*.md` の `lead` か件数（「◯技」「◯語」）だけを表示 | content/ を経由しない解説文を書かない（CLAUDE.md） |
| D-21 | 生成物の形 | `src/generated/index.json`（一覧）＋種別ごとの JSON。単語集 JSON に `used_in`（技からの逆リンク）を付ける | 初回報告 B-23。content-spec §5 に単語集の出力形式が無いため |
| D-24 | 一時停止の吹き出しの置き方 | 絵の上に重ねず、絵の上の帯（目・顔・肩）と下の帯（腹・膝・足）に、関節の x 座標の順で並べる。関節とは筆の引き出し線で結ぶ | 360px 幅でも吹き出しどうし・骨格と重ならない（T16 の「6つが正しい関節に乗る」を線で担保） |
| D-25 | トグルの深さの意味 | 深さ d で l1〜l(d) を上から積み重ねて表示（l(d) だけに差し替えない）。同じ吹き出しをもう一度タップ／「さらに詳しく」／ピンチアウトで1段深く。深さ5が上限 | 浅い段の要点を見ながら深い段を読める。A-8「l1〜l5 の深層表示」の読み |
| D-26 | 部位を開いている間の表示 | 吹き出しの帯を畳み、骨格の段を画面上部に sticky で留める。開いた部位に朱の輪と部位名の札。ズームは 1＋0.2×深さ（上限2.0）で、開いた部位の関節を中心に拡大 | 下の解説を読みながら骨格を見られる。旧 references/gesture-history.md の 1/1.6/2.4 倍は5段になって過大なため |
| D-27 | 技の巻物ストリップ | 画面幅の4倍の横長の帯を横スクロールし、中央の縦線の位置＝進捗。指を離して止まったら ±0.025 以内の kf にそろえる（CSS scroll-snap は使わない）。再生の等速は技の最初から最後まで7秒 | scroll-snap は再生停止時に帯だけが動いて進捗とずれたため、そろえ処理を自前にした |
| D-28 | 戻るボタンと画面遷移 | トグルの深さは history.state が正（開く・深く＝pushState、戻る＝popstate で1段、画面の「閉じる」＝history.back()）。用語リンクなどで画面を移る直前に、今の画面の状態（進捗・視点・スクロール・一覧の絞り込み）を今のエントリに保存し、戻ったら復元する | A-5 の depth スタック。多重操作は処理中フラグで無視する |
| D-29 | 部位の短い表示名 | 吹き出し・札は 目／顔／肩／腹／膝／足、トグル見出しは正式名（PART_LABEL） | 狭い帯に6つ並べるため |
| D-30 | 深さ5の背景 | B-5「深いほど和紙濃度 up」の最深段は #e9e1cf で止める | 重（#555）の一般指導文が 4.5:1 を保てる濃さの上限（#e9e1cf 上で 5.8:1） |
| D-31 | 画面の読み込み（T31） | 表紙だけ入口の JS に含め、技詳細・一覧・単語集・用語・章ページは開いたときに読み込む。HTML に静的な表紙（「合氣道」）を置き、アプリの最初の描画で取り除く | 入口 JS 189KB→78KB（gzip 71KB→30KB）。requirements「初期JSバンドル < 150KB gzip」 |
| D-32 | Web フォントの読み込み順（T31） | `html.fonts` が付くまで端末の明朝で表示。初回は画面と技データの表示後に付けて読み込み開始（font-display: swap で差し替え）。Service Worker の制御下（precache 済み）では HTML の段階で付け、最初から Web フォント | 3G で約1.8MB のフォントが JS・データより先に帯域を取らない。requirements「初回LCP < 2.5s（3G相当）」。2回目以降はちらつかない |
| D-33 | Service Worker の登録時期（T31） | ページの読み込み完了後に登録（vite-plugin-pwa の既定） | precache（約2.3MB）の取得が初回表示と帯域を取り合わない |
| D-34 | 生成物の一覧の分け方（T31、D-21 の追補） | `index.json`（起動時に読む）は技・基礎・章ページの一覧、単語集の語数、技が使う攻撃法の名前だけにする。単語集の一覧は `glossary-index.json` に分け、単語集・用語の画面のチャンクにだけ入れる | 単語集が214語になり、一覧データで入口 JS が 78KB→144KB に増えたため（分けた後 87KB） |
| D-35 | 1ファイル版（ディレクター依頼「完成品をhtmlにして」） | `npm run build:single` で `dist-single/aikido-scroll-app.html`（画面・原稿・フォントを埋め込んだ HTML 1つ、約3.9MB）を作る。Service Worker・manifest は持たず、JS は1本（遅延読み込みなし）、Web フォントは最初から使う。公開版（`npm run build`）のコードと出力は変えない | サーバー無しで開ける・ファイルで渡せる形。オフラインの自動キャッシュとホーム画面追加は公開版で行う |
| D-36 | 技アニメの 3D 化（2026-09-23 ディレクター決定「3D の作り方：A Three.js」） | 技アニメを Three.js（WebGL）の 3D にする。Unity は採らない。2D の SVG は、3D ポーズが無い技と WebGL が使えない端末向けに残す。CLAUDE.md 絶対ルール1・6 と animation-spec §13 を更新 | 2D の真横では奥行き・回転（転換・くぐり・足運び）が見えず「動きが意味不明」（ディレクター）。Three.js なら吹き出し・ピンチ・戻る・オフライン・GitHub Pages をそのまま使え、読み込みも技の画面を開いたときだけ（約135KB gzip）。Unity は Web 出力が数MB〜十数MB、Editor 作業が要る |
| D-37 | Jev（TypeSafe AI、2026-09-15 公開） | 見送り（ディレクター決定） | 文章を読んで選択肢・点数・はい/いいえを返すだけの判定専用 AI で、動き・3D・文章は作れない。アプリ内で使うと鍵を隠すサーバーが要り、オフラインで動かず、部員の入力が外部に送られる |
| D-38 | 3D の動きの出どころ | 当面は記述からの推定（`tools/pose3d-seed/`、`source: "estimate"`）。撮影ができたら動画から作った動き（`source: "mocap"`）に置き換える。まず一教（表）だけ 3D にし、他の5技は 2D のまま | 撮影は今はできない（ディレクター）。推定の動きは原稿の足運び・手の位置に合わせ、師範の校閲まで仮（docs/content-review-notes.md の 3D の項） |
| D-39 | 3D の関節とカメラ | 関節は 2D の15点＋顔の向き・帯の結び目・手の先・つま先の21点、左右は解剖学の l/r。カメラは「斜め（既定）・横・真上」の3つから選び、技の間は固定。部位の手前 f はカメラに近い側 | 位置だけで持つと補間と検証が一様。自由に回せるカメラはピンチ・ページのスクロールと指の操作がぶつかるので、決まった3方向にした |
| D-40 | 3D の読み込みと描画の手間（T31 の維持） | three.js は技詳細で、説明文と吹き出しを出して手が空いてから読み込む。描画用プログラムは材料ごとに分けて準備し、描くのは1コマに1回。画素密度 1.5倍以上の画面では MSAA を省く。視線の点線は描かない（「目」の吹き出しの起点が視線の上に乗る） | Lighthouse（ソフトウェア描画の Chromium・CPU 4倍遅延）で技詳細の Performance が 85 に下がったため。対策後 94〜96（手元の本番ビルド、5回） |
| D-41 | 推定の動きの注記（2026-09-23 ディレクターのレビュー「実際の動きと3Dモデルに大きな乖離」への裁定「推定だと明示して表示」） | 3D のポーズが記述からの推定（`source: "estimate"`）のあいだは、3D の絵の左上に「推定の動き：実際の動きと異なる所があります」を常に出し、読み上げの説明にも含める | 部員が推定の動きをそのまま覚えるのを防ぐ。カメラの切り替えや吹き出しは使えるまま |
| D-42 | 実際の動きとの差の埋め方（同レビューへの裁定「言葉で指摘→Claude が直す」） | 確認シート（Artifact「一教（表）3D 確認シート」）で、場面ごとに足運び・手の取り方・崩れ方・抑えの形を「合っている／違う／わからない」と「正しくは」で書いてもらい、Claude が読み取って `tools/pose3d-seed/ikkyo-omote.mjs` を直す。細かい角度は、撮影できたら動画から合わせる | 撮影は今はできない。大きな差は向き・左右・足の位置の取り違えが多く、言葉で直すのが一番早い |
